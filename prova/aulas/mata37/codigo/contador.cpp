#include <iostream>

using namespace std;

int main() {
	int num, contador = 0;

  while ((cin >> num) && num != 0) {
  	contador++;
  }
  cout << num << endl;
  // cout << contador << endl;

	return 0;
}