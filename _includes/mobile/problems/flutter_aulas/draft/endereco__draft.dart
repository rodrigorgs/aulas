// Crie uma classe `Cliente`, cujo construtor recebe
// os parâmetros posicionais `nome` e `endereco`, sendo
// apenas o primeiro obrigatório. Caso não seja fornecido,
// o endereço deve ser inicializado com `null`.
//
// O nome não pode ser alterado após a criação do objeto.
//
// Crie também a classe `Endereco`, com os atributos
// `cep` e `cidade`, ambos obrigatórios do tipo `String`.
//
// A seguir, crie uma função `criaEtiqueta`, que recebe
// um cliente e retorna uma string com o nome e o CEP
// do cliente, de acordo com os seguintes exemplos:
//
// Fulano de Tal - CEP 12345-678
// Sicrana Silva - CEP não informado
//
// Sugestão: use operadores como `?.` e `??` para lidar com nulos.

class Cliente {
  // ...
}

class Endereco {
  // ...
}

String criaEtiqueta(Cliente c) {
  return "";
}
