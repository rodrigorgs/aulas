// Crie uma função, chamada `area_retangulo`, que recebe
// dois parâmetros, `base` e `altura`, e retorna a área
// do retângulo.

// Exemplos de uso:
void main() {
  print(area_retangulo(1.5, 2.0)); // 3.0
  print(area_retangulo(10, 2)); // 20.0
}
